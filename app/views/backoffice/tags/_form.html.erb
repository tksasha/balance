<%= form_for [:backoffice, category, resource], remote: true do |f| %>
  <div class="form-group">
    <%= input(f, :name) %>
  </div>

  <div class="form-group mt-4">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
