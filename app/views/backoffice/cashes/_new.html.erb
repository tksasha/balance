<%= render 'form' %>

<div class="d-none" id="breadcrumbs">
  <%= breadcrumbs do %>
    <%= link_to 'Залишки', [:backoffice, :cashes], class: 'breadcrumb-item', data: { remote: true } %>

    <%= content_tag :span, 'Додавання залишка', class: 'breadcrumb-item active' %>
  <% end %>
</div>
