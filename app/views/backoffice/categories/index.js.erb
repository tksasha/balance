(function() {
  document.
    querySelector('.modal-title').
    innerHTML = '<%= j render 'breadcrumbs' %>';

  document.
    querySelector('.modal-body').
    innerHTML = '<%= j render 'index' %>';
}());
