<%= breadcrumbs do %>
  <%= link_to 'Категорії', [:backoffice, :categories], class: 'breadcrumb-item', data: { remote: true } %>

  <span class="breadcrumb-item active">Додавання категорії</span>
<% end %>

<%= render 'form' %>
