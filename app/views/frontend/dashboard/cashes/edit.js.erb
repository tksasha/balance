(function() {
  document.
    querySelector('.modal-title').
    innerText = '<%= t('modal.title.cashes.edit') %>';

  document
    .querySelector('.modal-body')
    .innerHTML = '<%= j render 'form' %>';

  bootstrap.Modal.Show();

  document.
    querySelector('#cash_formula').
    focus();
}());
