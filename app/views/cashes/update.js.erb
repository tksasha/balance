(function(){
  $('#<%= dom_id(resource) %>').replaceWith('<%= j render 'cash', cash: resource %>');

  $('.modal').modal('hide');

  SummarizeCashTables();

  $('#balance')
    .text('<%= money(dashboard.balance) %>')
    .trigger('balance.changed');
}());
