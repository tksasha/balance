<div class="<%= cash.css_class %>" id="<%= dom_id(cash) %>">
  <div class="cash-name">
    <%= cash.name %>
  </div>

  <div class="cash-sum">
    <%= link_to money(cash.sum), edit_cash_path(cash), data: { remote: true } %>
  </div>
</div>
