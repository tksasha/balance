<%= breadcrumbs do %>
  <%= link_to 'Залишки', :cashes, class: 'breadcrumb-item', data: { remote: true } %>

  <%= content_tag :span, 'Редагування залишку', class: 'breadcrumb-item active' %>
<% end %>

<%= render 'form' %>
