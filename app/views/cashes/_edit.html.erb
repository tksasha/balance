<%= breadcrumbs do %>
  <%= link_to 'Залишки', :cashes, class: 'breadcrumb', data: { remote: true } %>

  <%= content_tag :span, 'Редагування залишку', class: 'breadcrumb' %>
<% end %>

<%= render 'form' %>
