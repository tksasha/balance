(function() {
  $('.modal-header').hide();

  $('.modal-body').html('<%= j render 'edit' %>');

  $('.modal-dialog').removeClass('modal-lg');

  $('#cash_formula').focus();

  if(!$('.modal').hasClass('show')) {
    $('.modal').modal();
  }
}());
