(function() {
  $('.modal-body').html('<%= j render 'form' %>');

  $('.modal').find('.datepicker').datepicker(BOOTSTRAP_DATEPICKER_DEFAULTS);

  $('.modal').find('select').selectpicker();

  $('.modal').modal('show');
}());
